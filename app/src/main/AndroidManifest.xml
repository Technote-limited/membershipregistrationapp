<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dhl">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InventoryManagementActivity"></activity>
        <activity android:name=".OrderTrackingActivity" />
        <activity android:name=".OrderStatusActivity" />
        <activity android:name=".OrderPlacementActivity" />
        <activity android:name=".CardStatusWornOut" />
        <activity android:name=".CardStatusStolen" />
        <activity android:name=".CardStatusLost" />
        <activity android:name=".ManageCardsActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".SearchMembersActivity" />
        <activity android:name=".AgentLoginActivity" />
        <activity android:name=".AgentRegister" />
        <activity android:name=".barcode.BarCodeCaptureActivity" />
        <activity
            android:name=".AgentActivity"
            android:launchMode="singleTop" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".BoardingActivity" />
        <activity android:name=".camera.Camera" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>